<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>AI-ON Mentoría — Tu Mentor de Élite</title>
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@700;800&family=DM+Sans:wght@300;400;500;600&display=swap" rel="stylesheet">
<style>
:root{--negro:#050810;--tarjeta:#0C1525;--azul:#0055FF;--cyan:#00BBFF;--dorado:#F5A623;--naranja:#FF6B2B;--blanco:#ECF2FF;--gris:#5A7390;--gris2:#2E4560;--verde:#00D68F;--rojo:#FF4D6D;}
*{margin:0;padding:0;box-sizing:border-box;}
html{scroll-behavior:smooth;}
body{background:var(--negro);color:var(--blanco);font-family:'DM Sans',sans-serif;overflow-x:hidden;}
nav{position:sticky;top:0;z-index:200;display:flex;align-items:center;justify-content:space-between;padding:14px 24px;background:rgba(5,8,16,0.96);backdrop-filter:blur(20px);border-bottom:1px solid rgba(0,187,255,0.12);}
.logo{font-family:'Syne',sans-serif;font-weight:800;font-size:22px;letter-spacing:-1px;background:linear-gradient(135deg,var(--cyan),var(--azul));-webkit-background-clip:text;-webkit-text-fill-color:transparent;}
.logo span{-webkit-text-fill-color:var(--dorado);}
.nav-r{display:flex;align-items:center;gap:8px;}
.lang{padding:6px 8px;border-radius:8px;border:1px solid rgba(0,187,255,0.2);background:rgba(0,187,255,0.06);color:var(--cyan);font-size:11px;cursor:pointer;outline:none;}
.lang option{background:#071428;}
.btn-g{padding:8px 14px;border-radius:8px;border:1px solid rgba(0,187,255,0.25);background:transparent;color:var(--cyan);font-size:13px;cursor:pointer;}
.btn-s{padding:8px 16px;border-radius:8px;border:none;background:linear-gradient(135deg,var(--azul),var(--cyan));color:#fff;font-size:13px;font-weight:600;cursor:pointer;}
.modal-bg{position:fixed;inset:0;z-index:500;background:rgba(5,8,16,0.93);backdrop-filter:blur(12px);display:none;align-items:center;justify-content:center;padding:20px;}
.modal-bg.open{display:flex;}
.modal{background:var(--tarjeta);border:1px solid rgba(0,187,255,0.15);border-radius:22px;padding:28px 24px;width:100%;max-width:400px;}
.modal h3{font-family:'Syne',sans-serif;font-size:20px;font-weight:800;margin-bottom:6px;}
.modal-sub{font-size:13px;color:var(--gris);margin-bottom:18px;}
.mc{float:right;background:none;border:none;color:var(--gris);font-size:22px;cursor:pointer;}
.campo{margin-bottom:13px;}
.campo label{display:block;font-size:12px;color:var(--gris);margin-bottom:4px;}
.campo input,.campo select{width:100%;padding:11px 13px;border-radius:10px;border:1px solid rgba(255,255,255,0.08);background:rgba(255,255,255,0.04);color:var(--blanco);font-family:'DM Sans',sans-serif;font-size:13px;outline:none;}
.campo input::placeholder{color:var(--gris2);}
.campo select option{background:#071428;}
.modal-btn{width:100%;padding:13px;border-radius:10px;border:none;background:linear-gradient(135deg,var(--azul),var(--cyan));color:#fff;font-family:'DM Sans',sans-serif;font-size:14px;font-weight:600;cursor:pointer;margin-top:4px;}
.modal-sw{text-align:center;font-size:13px;color:var(--gris);margin-top:12px;}
.modal-sw a{color:var(--cyan);cursor:pointer;}
.err-box{background:rgba(255,77,109,0.1);border:1px solid rgba(255,77,109,0.2);border-radius:8px;padding:9px 13px;font-size:13px;color:var(--rojo);margin-bottom:11px;display:none;}

/* HERO DESKTOP 2 COLUMNAS */
.hero{position:relative;min-height:100vh;display:grid;grid-template-columns:1fr 1fr;align-items:center;gap:50px;padding:90px 50px 50px;overflow:hidden;}
.hero-bg{position:absolute;inset:0;z-index:0;background:radial-gradient(ellipse at 65% 25%,rgba(0,85,255,0.15) 0%,transparent 55%),radial-gradient(ellipse at 15% 75%,rgba(0,187,255,0.08) 0%,transparent 50%);}
.hero-grid{position:absolute;inset:0;z-index:0;background-image:linear-gradient(rgba(0,187,255,0.03) 1px,transparent 1px),linear-gradient(90deg,rgba(0,187,255,0.03) 1px,transparent 1px);background-size:42px 42px;}
.nodo{position:absolute;border-radius:50%;animation:nP 5s ease-in-out infinite;}
@keyframes nP{0%,100%{opacity:0;}50%{opacity:1;transform:scale(2.8);}}
.n1{width:5px;height:5px;background:var(--cyan);top:14%;left:8%;animation-delay:0s;}
.n2{width:4px;height:4px;background:var(--dorado);top:32%;left:52%;animation-delay:1.2s;}
.n3{width:6px;height:6px;background:var(--cyan);top:65%;left:32%;animation-delay:2.4s;}
.n4{width:4px;height:4px;background:var(--naranja);top:20%;left:80%;animation-delay:3.6s;}
.hero-left{position:relative;z-index:2;}
.hero-right{position:relative;z-index:2;}
.badge{display:inline-flex;align-items:center;gap:7px;padding:5px 14px;border-radius:100px;border:1px solid rgba(0,187,255,0.28);background:rgba(0,187,255,0.07);font-size:11px;color:var(--cyan);font-weight:600;letter-spacing:1.2px;text-transform:uppercase;margin-bottom:20px;}
.dot-v{width:7px;height:7px;border-radius:50%;background:var(--verde);box-shadow:0 0 9px var(--verde);animation:blink 1.6s infinite;display:inline-block;}
@keyframes blink{0%,100%{opacity:1;}50%{opacity:.2;}}
.hero h1{font-family:'Syne',sans-serif;font-size:clamp(32px,4vw,56px);font-weight:800;line-height:1.05;letter-spacing:-2px;margin-bottom:18px;}
.gc{background:linear-gradient(120deg,var(--cyan),var(--azul));-webkit-background-clip:text;-webkit-text-fill-color:transparent;}
.gd{background:linear-gradient(120deg,var(--dorado),var(--naranja));-webkit-background-clip:text;-webkit-text-fill-color:transparent;}
.hero-sub{font-size:16px;color:var(--gris);line-height:1.75;font-weight:300;margin-bottom:28px;}
.hero-btns{display:flex;gap:12px;flex-wrap:wrap;margin-bottom:36px;}
.btn-main{padding:14px 26px;border-radius:12px;border:none;background:linear-gradient(135deg,var(--azul),var(--cyan));color:#fff;font-family:'DM Sans',sans-serif;font-size:15px;font-weight:600;cursor:pointer;box-shadow:0 6px 24px rgba(0,136,255,0.35);}
.btn-sec{padding:14px 22px;border-radius:12px;border:1px solid rgba(0,187,255,0.3);background:transparent;color:var(--blanco);font-family:'DM Sans',sans-serif;font-size:15px;cursor:pointer;}
.hero-stats{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;}
.hs{background:var(--tarjeta);border:1px solid rgba(255,255,255,0.06);border-radius:12px;padding:16px;text-align:center;}
.hs-n{font-family:'Syne',sans-serif;font-size:26px;font-weight:800;background:linear-gradient(135deg,var(--cyan),var(--azul));-webkit-background-clip:text;-webkit-text-fill-color:transparent;line-height:1;margin-bottom:4px;}
.hs-l{font-size:11px;color:var(--gris);}

/* MENTOR */
.mentor-box{background:var(--tarjeta);border:1px solid rgba(0,187,255,0.14);border-radius:20px;overflow:hidden;box-shadow:0 24px 70px rgba(0,0,0,0.55);}
.mentor-head{background:rgba(0,187,255,0.055);border-bottom:1px solid rgba(0,187,255,0.1);padding:13px 18px;display:flex;align-items:center;gap:12px;}
.orbe{width:36px;height:36px;border-radius:50%;background:linear-gradient(135deg,var(--azul),var(--cyan));display:flex;align-items:center;justify-content:center;font-family:'Syne',sans-serif;font-weight:800;font-size:13px;color:#fff;}
.mentor-nombre{font-family:'Syne',sans-serif;font-weight:700;font-size:14px;}
.mentor-estado{font-size:11px;color:var(--verde);}
.en-vivo{margin-left:auto;display:flex;align-items:center;gap:5px;font-size:11px;color:var(--verde);font-weight:600;}
.chat-msgs{padding:16px;display:flex;flex-direction:column;gap:10px;height:260px;overflow-y:auto;}
.chat-msgs::-webkit-scrollbar{width:3px;}
.chat-msgs::-webkit-scrollbar-thumb{background:rgba(255,255,255,0.1);border-radius:2px;}
.bbl-ia{background:rgba(0,187,255,0.07);border:1px solid rgba(0,187,255,0.16);border-radius:4px 14px 14px 14px;padding:11px 14px;font-size:13px;line-height:1.65;max-width:88%;}
.bbl-ia strong{color:var(--cyan);}
.bbl-u{background:rgba(0,85,255,0.18);border:1px solid rgba(0,136,255,0.22);border-radius:14px 4px 14px 14px;padding:10px 14px;font-size:13px;margin-left:auto;max-width:80%;}
.bbl-file{background:rgba(245,166,35,0.08);border:1px solid rgba(245,166,35,0.2);border-radius:4px 14px 14px 14px;padding:10px 14px;font-size:12px;color:var(--dorado);max-width:88%;}
.typing{display:flex;gap:4px;padding:11px 14px;width:fit-content;}
.typing span{width:6px;height:6px;border-radius:50%;background:var(--cyan);animation:td 1.2s infinite;}
.typing span:nth-child(2){animation-delay:.2s;}
.typing span:nth-child(3){animation-delay:.4s;}
@keyframes td{0%,100%{transform:translateY(0);opacity:.3;}50%{transform:translateY(-4px);opacity:1;}}
.input-area{border-top:1px solid rgba(255,255,255,0.055);}
.file-bar{display:flex;gap:6px;padding:8px 14px 0;flex-wrap:wrap;}
.file-btn{display:flex;align-items:center;gap:5px;padding:5px 10px;border-radius:8px;border:1px solid rgba(255,255,255,0.08);background:rgba(255,255,255,0.04);color:var(--gris);font-size:11px;cursor:pointer;transition:all .2s;}
.file-btn:hover{border-color:rgba(0,187,255,0.3);color:var(--cyan);}
.file-btn input{display:none;}
.file-preview{padding:6px 14px;display:none;}
.file-chip{display:inline-flex;align-items:center;gap:5px;padding:4px 10px;border-radius:100px;background:rgba(245,166,35,0.1);border:1px solid rgba(245,166,35,0.25);font-size:11px;color:var(--dorado);}
.file-chip button{background:none;border:none;color:var(--dorado);cursor:pointer;font-size:13px;}
.chat-input{display:flex;gap:8px;padding:10px 14px 13px;}
.chat-input input{flex:1;background:rgba(255,255,255,0.04);border:1px solid rgba(255,255,255,0.09);border-radius:10px;padding:10px 14px;color:var(--blanco);font-family:'DM Sans',sans-serif;font-size:13px;outline:none;}
.chat-input input::placeholder{color:var(--gris2);}
.btn-send{width:40px;height:40px;border-radius:10px;border:none;background:linear-gradient(135deg,var(--azul),var(--cyan));color:#fff;font-size:17px;cursor:pointer;display:flex;align-items:center;justify-content:center;flex-shrink:0;}

/* SECCIONES */
.seccion{padding:70px 24px;max-width:1100px;margin:0 auto;}
.sec-tag{font-size:10px;font-weight:700;letter-spacing:2px;text-transform:uppercase;color:var(--cyan);margin-bottom:10px;display:block;}
.sec-title{font-family:'Syne',sans-serif;font-size:clamp(26px,3.5vw,40px);font-weight:800;letter-spacing:-1.5px;line-height:1.08;margin-bottom:12px;}
.sec-sub{font-size:15px;color:var(--gris);line-height:1.7;margin-bottom:28px;font-weight:300;}
.grid-4{display:grid;grid-template-columns:repeat(4,1fr);gap:16px;}
.mod-card{background:var(--tarjeta);border:1px solid rgba(255,255,255,0.06);border-radius:18px;padding:24px;cursor:pointer;transition:all .3s;}
.mod-card:hover{transform:translateY(-4px);border-color:rgba(0,187,255,0.2);}
.mod-ic{width:46px;height:46px;border-radius:13px;display:flex;align-items:center;justify-content:center;font-size:20px;margin-bottom:14px;}
.ic-b{background:rgba(0,85,255,0.15);}
.ic-c{background:rgba(0,187,255,0.12);}
.ic-g{background:rgba(245,166,35,0.12);}
.ic-v{background:rgba(0,214,143,0.12);}
.mod-title{font-family:'Syne',sans-serif;font-size:16px;font-weight:700;margin-bottom:7px;}
.mod-desc{font-size:12px;color:var(--gris);line-height:1.6;}
.free-sec{background:var(--tarjeta);border-top:1px solid rgba(0,187,255,0.08);border-bottom:1px solid rgba(0,187,255,0.08);padding:70px 24px;}
.free-inner{max-width:1100px;margin:0 auto;}
.grid-2{display:grid;grid-template-columns:1fr 1fr;gap:20px;}
.cv-card{border-radius:18px;padding:24px;position:relative;overflow:hidden;}
.cv-free{background:rgba(90,115,144,0.07);border:1px solid rgba(90,115,144,0.18);}
.cv-prem{background:linear-gradient(160deg,rgba(0,85,255,0.12),var(--tarjeta));border:1px solid rgba(0,187,255,0.28);}
.cv-prem::before{content:'';position:absolute;top:0;left:0;right:0;height:2px;background:linear-gradient(90deg,var(--azul),var(--cyan));}
.cv-tipo{font-family:'Syne',sans-serif;font-weight:800;font-size:18px;margin-bottom:6px;}
.cv-desc{font-size:13px;color:var(--gris);margin-bottom:15px;line-height:1.6;}
.cv-list{list-style:none;display:flex;flex-direction:column;gap:8px;margin-bottom:16px;}
.cv-list li{display:flex;align-items:center;gap:8px;font-size:13px;}
.ck{color:var(--verde);}
.no{color:var(--gris2);}
.cv-prev{border-radius:10px;padding:13px;position:relative;overflow:hidden;background:rgba(255,255,255,0.03);border:1px solid rgba(255,255,255,0.06);font-size:11px;color:var(--gris);line-height:1.9;margin-bottom:15px;}
.wm{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;font-family:'Syne',sans-serif;font-size:26px;font-weight:800;color:rgba(0,187,255,0.07);transform:rotate(-22deg);pointer-events:none;letter-spacing:4px;}
.cv-btn{width:100%;padding:12px;border-radius:10px;border:none;font-family:'DM Sans',sans-serif;font-size:13px;font-weight:600;cursor:pointer;}
.cv-btn-f{background:rgba(90,115,144,0.15);color:var(--gris);}
.cv-btn-p{background:linear-gradient(135deg,var(--azul),var(--cyan));color:#fff;}
.planes-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:18px;}
.plan-card{background:var(--tarjeta);border:1px solid rgba(255,255,255,0.06);border-radius:18px;padding:24px;position:relative;overflow:hidden;transition:transform .3s;}
.plan-card:hover{transform:translateY(-4px);}
.plan-hot{border-color:rgba(0,187,255,0.28);background:linear-gradient(160deg,rgba(0,85,255,0.12),var(--tarjeta));}
.plan-hot::before{content:'';position:absolute;top:0;left:0;right:0;height:2px;background:linear-gradient(90deg,var(--azul),var(--cyan));}
.plan-badge{position:absolute;top:14px;right:14px;background:linear-gradient(135deg,var(--dorado),var(--naranja));color:#fff;font-size:10px;font-weight:700;padding:3px 10px;border-radius:100px;}
.plan-name{font-family:'Syne',sans-serif;font-size:20px;font-weight:800;margin-bottom:4px;}
.plan-price{font-family:'Syne',sans-serif;font-size:44px;font-weight:800;background:linear-gradient(135deg,var(--cyan),var(--azul));-webkit-background-clip:text;-webkit-text-fill-color:transparent;line-height:1;margin:11px 0 4px;}
.plan-period{font-size:12px;color:var(--gris);margin-bottom:17px;}
.plan-list{list-style:none;display:flex;flex-direction:column;gap:9px;margin-bottom:20px;}
.plan-list li{display:flex;align-items:center;gap:8px;font-size:13px;color:#B8CDE5;}
.pb-s{width:100%;padding:13px;border-radius:10px;border:none;background:linear-gradient(135deg,var(--azul),var(--cyan));color:#fff;font-family:'DM Sans',sans-serif;font-size:14px;font-weight:600;cursor:pointer;}
.pb-g{width:100%;padding:13px;border-radius:10px;border:1px solid rgba(0,187,255,0.25);background:transparent;color:var(--cyan);font-family:'DM Sans',sans-serif;font-size:14px;cursor:pointer;}
.cta-final{background:linear-gradient(135deg,#060E2E,#081A4A);border:1px solid rgba(0,187,255,0.18);border-radius:24px;padding:60px 30px;text-align:center;margin:0 24px 60px;}
.cta-title{font-family:'Syne',sans-serif;font-size:clamp(26px,4vw,40px);font-weight:800;letter-spacing:-1px;margin-bottom:14px;}
.cta-sub{font-size:15px;color:var(--gris);line-height:1.7;margin-bottom:28px;max-width:480px;margin-left:auto;margin-right:auto;}
footer{background:rgba(5,8,16,0.98);border-top:1px solid rgba(0,187,255,0.09);padding:40px 24px;text-align:center;}
.foot-logo{font-family:'Syne',sans-serif;font-weight:800;font-size:22px;background:linear-gradient(135deg,var(--cyan),var(--azul));-webkit-background-clip:text;-webkit-text-fill-color:transparent;margin-bottom:6px;}
.foot-logo span{-webkit-text-fill-color:var(--dorado);}
.foot-links{display:flex;justify-content:center;gap:20px;margin:10px 0;}
.foot-links a{font-size:13px;color:var(--gris);text-decoration:none;}
.foot-copy{font-size:11px;color:rgba(90,115,144,0.4);margin-top:10px;}
.toast{position:fixed;bottom:22px;left:50%;transform:translateX(-50%) translateY(90px);background:var(--tarjeta);border-radius:12px;padding:12px 20px;font-size:13px;transition:transform .35s;z-index:9999;white-space:nowrap;}
.toast.show{transform:translateX(-50%) translateY(0);}
.toast.ok{border:1px solid rgba(0,214,143,0.3);color:var(--verde);}
.toast.err{border:1px solid rgba(255,77,109,0.3);color:var(--rojo);}
.pill{display:flex;align-items:center;gap:8px;padding:6px 12px;border-radius:100px;border:1px solid rgba(0,187,255,0.15);background:rgba(0,187,255,0.05);cursor:pointer;position:relative;}
.av{width:26px;height:26px;border-radius:50%;background:linear-gradient(135deg,var(--azul),var(--cyan));display:flex;align-items:center;justify-content:center;font-weight:800;font-size:11px;color:#fff;}
.menu-drop{position:absolute;top:calc(100% + 8px);right:0;background:var(--tarjeta);border:1px solid rgba(0,187,255,0.12);border-radius:12px;padding:8px;min-width:150px;display:none;z-index:300;}
.pill:hover .menu-drop{display:block;}
.di{padding:8px 11px;border-radius:8px;font-size:13px;cursor:pointer;}
.di:hover{background:rgba(0,187,255,0.08);}
.di.danger:hover{color:var(--rojo);}
@media(max-width:860px){
  .hero{grid-template-columns:1fr;padding:75px 18px 40px;gap:30px;}
  .grid-4{grid-template-columns:1fr 1fr;}
  .grid-2{grid-template-columns:1fr;}
  .planes-grid{grid-template-columns:1fr;}
  .cta-final{margin:0 18px 40px;padding:40px 18px;}
  .seccion{padding:50px 18px;}
  .free-sec{padding:50px 18px;}
  nav{padding:12px 16px;}
}
@media(max-width:540px){.grid-4{grid-template-columns:1fr;}.hero-stats{grid-template-columns:1fr 1fr 1fr;}}
</style>
</head>
<body>
<div class="toast" id="toast"></div>
<div class="modal-bg" id="mLogin">
  <div class="modal">
    <button class="mc" onclick="cerrar('Login')">×</button>
    <h3>Bienvenido de vuelta</h3>
    <p class="modal-sub">Tu mentor te está esperando.</p>
    <div class="err-box" id="errLogin"></div>
    <div class="campo"><label>Email</label><input type="email" id="lEmail" placeholder="tu@email.com"></div>
    <div class="campo"><label>Contraseña</label><input type="password" id="lPass" placeholder="Tu contraseña"></div>
    <button class="modal-btn" onclick="login()">Ingresar →</button>
    <div class="modal-sw">¿No tenés cuenta? <a onclick="cambiar('Login','Reg')">Registrate gratis</a></div>
  </div>
</div>
<div class="modal-bg" id="mReg">
  <div class="modal">
    <button class="mc" onclick="cerrar('Reg')">×</button>
    <h3>Crear cuenta gratis</h3>
    <p class="modal-sub">Tu mentor de élite te espera.</p>
    <div class="err-box" id="errReg"></div>
    <div class="campo"><label>Nombre completo</label><input type="text" id="rNombre" placeholder="María González"></div>
    <div class="campo"><label>Email</label><input type="email" id="rEmail" placeholder="maria@email.com"></div>
    <div class="campo"><label>Contraseña</label><input type="password" id="rPass" placeholder="Mínimo 6 caracteres"></div>
    <div class="campo"><label>País</label>
      <select id="rPais">
        <option value="AR">🇦🇷 Argentina</option>
        <option value="MX">🇲🇽 México</option>
        <option value="CO">🇨🇴 Colombia</option>
        <option value="CL">🇨🇱 Chile</option>
        <option value="PE">🇵🇪 Perú</option>
        <option value="ES">🇪🇸 España</option>
        <option value="US">🇺🇸 Estados Unidos</option>
        <option value="BR">🇧🇷 Brasil</option>
        <option value="OTHER">🌍 Otro</option>
      </select>
    </div>
    <button class="modal-btn" onclick="registro()">Crear cuenta gratis →</button>
    <div class="modal-sw">¿Ya tenés cuenta? <a onclick="cambiar('Reg','Login')">Ingresá</a></div>
  </div>
</div>
<nav>
  <div class="logo">AI<span>-ON</span> <span style="font-size:14px;font-weight:600;-webkit-text-fill-color:var(--gris);margin-left:4px;">Mentoría</span></div>
  <div class="nav-r">
    <select class="lang" id="langSel" onchange="cambiarIdioma(this.value)">
      <option value="es">🇪🇸 ES</option>
      <option value="en">🇺🇸 EN</option>
      <option value="pt">🇧🇷 PT</option>
      <option value="fr">🇫🇷 FR</option>
    </select>
    <div id="authBtns" style="display:flex;gap:8px;">
      <button class="btn-g" onclick="abrir('Login')">Ingresar</button>
      <button class="btn-s" onclick="abrir('Reg')">Empezar gratis</button>
    </div>
    <div id="userPill" class="pill" style="display:none;">
      <div class="av" id="userAv">U</div>
      <span id="userName" style="font-size:12px;font-weight:500;">Usuario</span>
      <div class="menu-drop">
        <div class="di">👤 Mi perfil</div>
        <div class="di">📄 Mis CVs</div>
        <div class="di danger" onclick="logout()">🚪 Salir</div>
      </div>
    </div>
  </div>
</nav>
<section class="hero">
  <div class="hero-bg"></div><div class="hero-grid"></div>
  <div class="nodo n1"></div><div class="nodo n2"></div><div class="nodo n3"></div><div class="nodo n4"></div>
  <div class="hero-left">
    <div class="badge"><span class="dot-v"></span><span id="badgeText">Tu Mentor de Élite · IA Real</span></div>
    <h1>El futuro<br>del empleo<br><span class="gc">está encendido.</span><br><span class="gd">AI-ON.</span></h1>
    <p class="hero-sub" id="heroSub">AI-ON Mentoría es el puente inteligente entre tu talento y la oportunidad que merecés. Un mentor de élite en tu bolsillo, disponible 24/7.</p>
    <div class="hero-btns">
      <button class="btn-main" onclick="abrir('Reg')">Crear mi CV gratis</button>
      <button class="btn-sec">Ver cómo funciona ↓</button>
    </div>
    <div class="hero-stats">
      <div class="hs"><div class="hs-n">+3x</div><div class="hs-l">Más chances</div></div>
      <div class="hs"><div class="hs-n">24/7</div><div class="hs-l">Mentor IA</div></div>
      <div class="hs"><div class="hs-n">$0</div><div class="hs-l">Para empezar</div></div>
    </div>
  </div>
  <div class="hero-right">
    <div class="mentor-box">
      <div class="mentor-head">
        <div class="orbe">AI</div>
        <div><div class="mentor-nombre">AI-ON Mentoría</div><div class="mentor-estado">● Activo ahora</div></div>
        <div class="en-vivo"><span class="dot-v"></span> LIVE</div>
      </div>
      <div class="chat-msgs" id="chatMsgs">
        <div class="bbl-ia">¡Hola! Soy <strong>AI-ON Mentoría</strong>, tu mentor de élite. Podés escribirme o subir tu CV para que lo analice. ¿Por dónde empezamos? 💼</div>
      </div>
      <div class="input-area">
        <div class="file-bar">
          <label class="file-btn">📄 CV (PDF/Word)<input type="file" accept=".pdf,.doc,.docx" onchange="subirArchivo(this,'cv')"></label>
          <label class="file-btn">🖼️ Foto/Imagen<input type="file" accept="image/*" onchange="subirArchivo(this,'img')"></label>
          <label class="file-btn">📋 Referencia<input type="file" accept=".pdf,.doc,.docx,image/*" onchange="subirArchivo(this,'ref')"></label>
        </div>
        <div class="file-preview" id="filePreview"></div>
        <div class="chat-input">
          <input type="text" id="chatIn" placeholder="Escribile a AI-ON Mentoría...">
          <button class="btn-send" onclick="enviar()">↑</button>
        </div>
      </div>
    </div>
  </div>
</section>
<div class="seccion">
  <span class="sec-tag">// Plataforma completa</span>
  <h2 class="sec-title">Todo lo que necesitás<br>en un solo lugar.</h2>
  <p class="sec-sub">Cuatro módulos para acompañarte en cada etapa de tu carrera.</p>
  <div class="grid-4">
    <div class="mod-card"><div class="mod-ic ic-b">👤</div><div class="mod-title">Tu Perfil Laboral</div><p class="mod-desc">Creá o mejorá tu CV con IA. Tus habilidades reales presentadas de forma profesional.</p></div>
    <div class="mod-card"><div class="mod-ic ic-c">🏢</div><div class="mod-title">Para Empresas</div><p class="mod-desc">Encontrá el candidato ideal. Perfiles verificados y optimizados por inteligencia artificial.</p></div>
    <div class="mod-card"><div class="mod-ic ic-g">🤖</div><div class="mod-title">Mentor AI-ON</div><p class="mod-desc">Coach de RRHH disponible 24/7. Entrevistas, consultas y acompañamiento profesional.</p></div>
    <div class="mod-card"><div class="mod-ic ic-v">📚</div><div class="mod-title">Academia</div><p class="mod-desc">Cursos recomendados según tu perfil. Las habilidades que el mercado demanda hoy.</p></div>
  </div>
</div>
<div class="free-sec">
  <div class="free-inner">
    <span class="sec-tag">// Modelo de acceso</span>
    <h2 class="sec-title">Gratis o Premium.<br>Vos elegís.</h2>
    <p class="sec-sub">Todos los CVs son honestos y verificados. La diferencia está en la presentación.</p>
    <div class="grid-2">
      <div class="cv-card cv-free">
        <div class="cv-tipo">CV Gratuito</div>
        <p class="cv-desc">Con sello AI-ON. Garantía de veracidad ante el empleador.</p>
        <ul class="cv-list">
          <li><span class="ck">✓</span> CV optimizado con IA</li>
          <li><span class="ck">✓</span> Sello de veracidad AI-ON</li>
          <li><span class="ck">✓</span> Descarga en PDF</li>
          <li><span class="no">—</span> <span style="color:var(--gris)">Sin marca de agua (Premium)</span></li>
        </ul>
        <div class="cv-prev">
          <div class="wm">AI-ON</div>
          <strong>María González</strong> · Diseñadora UX<br>Buenos Aires, Argentina<br>─────────────<br>Experiencia · Habilidades · Logros<br><span style="color:var(--cyan);font-size:11px">✓ Verificado por AI-ON</span>
        </div>
        <button class="cv-btn cv-btn-f" onclick="abrir('Reg')">Generar CV gratis</button>
      </div>
      <div class="cv-card cv-prem">
        <div class="cv-tipo" style="color:var(--cyan)">CV Premium ⭐</div>
        <p class="cv-desc">Sin marca de agua. Diseño profesional para cualquier empresa del mundo.</p>
        <ul class="cv-list">
          <li><span class="ck">✓</span> Diseño premium sin marcas</li>
          <li><span class="ck">✓</span> Optimizado ATS internacional</li>
          <li><span class="ck">✓</span> Múltiples idiomas</li>
          <li><span class="ck">✓</span> Actualizaciones ilimitadas</li>
          <li><span class="ck">✓</span> Análisis de referencias</li>
        </ul>
        <button class="cv-btn cv-btn-p" onclick="abrir('Reg')">Obtener CV Premium →</button>
      </div>
    </div>
  </div>
</div>
<div class="seccion">
  <span class="sec-tag">// Planes</span>
  <h2 class="sec-title">Empezá gratis,<br>crecé sin límites.</h2>
  <p class="sec-sub">Sin tarjeta de crédito para comenzar.</p>
  <div class="planes-grid">
    <div class="plan-card">
      <div class="plan-name">Gratis</div><div class="plan-price">$0</div><div class="plan-period">para siempre</div>
      <ul class="plan-list"><li><span class="ck">✓</span> 1 CV con sello AI-ON</li><li><span class="ck">✓</span> 10 consultas/mes</li><li><span class="ck">✓</span> Cursos gratuitos</li><li><span class="ck">✓</span> Postulaciones ilimitadas</li></ul>
      <button class="pb-g" onclick="abrir('Reg')">Comenzar gratis</button>
    </div>
    <div class="plan-card plan-hot">
      <div class="plan-badge">⭐ Popular</div>
      <div class="plan-name">Pro</div><div class="plan-price">$9</div><div class="plan-period">por mes</div>
      <ul class="plan-list"><li><span class="ck">✓</span> CVs Premium ilimitados</li><li><span class="ck">✓</span> Mentor IA sin límites</li><li><span class="ck">✓</span> Simulación entrevistas</li><li><span class="ck">✓</span> Cursos premium</li><li><span class="ck">✓</span> Análisis referencias</li></ul>
      <button class="pb-s" onclick="abrir('Reg')">Empezar con Pro</button>
    </div>
    <div class="plan-card">
      <div class="plan-name">Empresa</div><div class="plan-price">$49</div><div class="plan-period">por mes</div>
      <ul class="plan-list"><li><span class="ck">✓</span> Publicaciones ilimitadas</li><li><span class="ck">✓</span> Matching IA candidatos</li><li><span class="ck">✓</span> Panel de gestión</li><li><span class="ck">✓</span> Soporte prioritario</li></ul>
      <button class="pb-g" onclick="abrir('Reg')">Para empresas</button>
    </div>
  </div>
</div>
<div class="cta-final">
  <div class="cta-title">Tu próximo trabajo<br>empieza hoy.</div>
  <p class="cta-sub">Personas de toda Latinoamérica ya están usando AI-ON Mentoría para conseguir el empleo que merecen.</p>
  <button class="btn-main" style="margin:0 auto;display:block;" onclick="abrir('Reg')">Crear mi cuenta gratis →</button>
</div>
<footer>
  <div class="foot-logo">AI<span>-ON</span></div>
  <div style="font-size:13px;color:var(--gris);margin-bottom:8px;">Mentoría · Tu Mentor de Élite · IA Real · Global</div>
  <div class="foot-links"><a href="#">Términos</a><a href="#">Privacidad</a><a href="#">Contacto</a><a href="#">Empresas</a></div>
  <div class="foot-copy">© 2026 AI-ON Mentoría — Todos los derechos reservados</div>
</footer>
<script>
let usuarios=JSON.parse(localStorage.getItem('aion_u')||'[]');
let userActual=JSON.parse(localStorage.getItem('aion_ua')||'null');
let historial=[];
let idioma='es';
let archivoActual=null;
const i18n={
  es:{badge:'Tu Mentor de Élite · IA Real',sub:'AI-ON Mentoría es el puente inteligente entre tu talento y la oportunidad que merecés.',sistema:`Sos AI-ON Mentoría, mentor de élite en RRHH y desarrollo de carrera. Actuás como coach empático basado en psicología organizacional.
REGLAS:
- NUNCA inventés datos ni experiencias
- Solo trabajás con info que el usuario provea
- Hacé UNA pregunta a la vez
- Analizás CVs, imágenes y documentos adjuntos
- Tono: profesional, cálido, empático
- Respondé en español`},
  en:{badge:'Your Elite Mentor · Real AI',sub:'AI-ON Mentoring is the intelligent bridge between your talent and the opportunity you deserve.',sistema:`You are AI-ON Mentoring, elite HR mentor. RULES: NEVER invent data. Ask ONE question at a time. Analyze uploaded documents. Be empathetic. Reply in English.`},
  pt:{badge:'Seu Mentor de Elite · IA Real',sub:'AI-ON Mentoria é a ponte inteligente entre seu talento e a oportunidade que você merece.',sistema:`Você é AI-ON Mentoria, mentor de elite em RH. REGRAS: NUNCA invente dados. Faça UMA pergunta. Analise documentos. Responda em português.`},
  fr:{badge:'Votre Mentor Élite · IA Réelle',sub:"AI-ON Mentorat est le pont intelligent entre votre talent et l'opportunité que vous méritez.",sistema:`Vous êtes AI-ON Mentorat, mentor élite en RH. RÈGLES: Ne jamais inventer. Posez UNE question. Analysez les documents. Répondez en français.`}
};
function cambiarIdioma(lang){
  idioma=lang;
  const d=i18n[lang];
  document.getElementById('badgeText').textContent=d.badge;
  document.getElementById('heroSub').textContent=d.sub;
  historial=[];
}
function subirArchivo(input,tipo){
  const archivo=input.files[0];
  if(!archivo)return;
  const tipos={cv:'📄 CV',img:'🖼️ Imagen',ref:'📋 Referencia'};
  const preview=document.getElementById('filePreview');
  preview.style.display='block';
  preview.innerHTML=`<span class="file-chip">${tipos[tipo]}: ${archivo.name} <button onclick="quitarArchivo()">×</button></span>`;
  const reader=new FileReader();
  reader.onload=function(e){
    archivoActual={nombre:archivo.name,tipo,datos:e.target.result,esImagen:archivo.type.startsWith('image/')};
    const msgs=document.getElementById('chatMsgs');
    const div=document.createElement('div');
    div.className='bbl-file';
    div.textContent=`📎 ${tipos[tipo]} cargado: ${archivo.name} — AI-ON lo va a analizar`;
    msgs.appendChild(div);
    msgs.scrollTop=msgs.scrollHeight;
  };
  if(archivo.type.startsWith('image/')){reader.readAsDataURL(archivo);}
  else{reader.readAsText(archivo);}
}
function quitarArchivo(){
  archivoActual=null;
  const p=document.getElementById('filePreview');
  p.style.display='none';p.innerHTML='';
}
async function enviar(){
  const input=document.getElementById('chatIn');
  const msgs=document.getElementById('chatMsgs');
  const texto=input.value.trim();
  if(!texto&&!archivoActual)return;
  if(texto){
    const d=document.createElement('div');
    d.className='bbl-u';d.textContent=texto;
    msgs.appendChild(d);
  }
  input.value='';
  const typing=document.createElement('div');
  typing.className='typing';
  typing.innerHTML='<span></span><span></span><span></span>';
  msgs.appendChild(typing);
  msgs.scrollTop=msgs.scrollHeight;
  let contenido;
  if(archivoActual&&archivoActual.esImagen){
    const base64=archivoActual.datos.split(',')[1];
    const mediaType=archivoActual.datos.split(';')[0].split(':')[1];
    contenido=[{type:'image',source:{type:'base64',media_type:mediaType,data:base64}},{type:'text',text:texto||'Analizá esta imagen y dame feedback profesional para mejorar mi perfil laboral.'}];
  }else if(archivoActual){
    contenido=(texto?texto+'\n\n':'')+'[ARCHIVO ADJUNTO: '+archivoActual.nombre+']\n\n'+archivoActual.datos.substring(0,3000);
  }else{
    contenido=texto;
  }
  historial.push({role:'user',content:contenido});
  if(archivoActual)quitarArchivo();
  try{
    const resp=await fetch('https://api.anthropic.com/v1/messages',{
      method:'POST',
      headers:{'Content-Type':'application/json'},
      body:JSON.stringify({model:'claude-sonnet-4-20250514',max_tokens:1500,system:i18n[idioma].sistema,messages:historial})
    });
    const datos=await resp.json();
    typing.remove();
    const rt=datos.content?.[0]?.text||'Error al procesar. Intentá de nuevo.';
    historial.push({role:'assistant',content:rt});
    const d=document.createElement('div');
    d.className='bbl-ia';
    d.innerHTML=rt.replace(/\*\*(.*?)\*\*/g,'<strong>$1</strong>').replace(/\*(.*?)\*/g,'<em>$1</em>').replace(/\n/g,'<br>');
    msgs.appendChild(d);
  }catch(e){
    typing.remove();
    const d=document.createElement('div');
    d.className='bbl-ia';
    d.textContent='⚠️ No pude conectarme. Verificá tu conexión.';
    msgs.appendChild(d);
  }
  msgs.scrollTop=msgs.scrollHeight;
}
document.getElementById('chatIn').addEventListener('keypress',e=>{if(e.key==='Enter')enviar();});
function registro(){
  const n=document.getElementById('rNombre').value.trim();
  const e=document.getElementById('rEmail').value.trim();
  const p=document.getElementById('rPass').value;
  const pa=document.getElementById('rPais').value;
  const err=document.getElementById('errReg');
  if(!n||!e||!p)return mostrarErr(err,'Completá todos los campos.');
  if(p.length<6)return mostrarErr(err,'Contraseña mínimo 6 caracteres.');
  if(usuarios.find(u=>u.email===e))return mostrarErr(err,'Email ya registrado.');
  const u={id:Date.now(),nombre:n,email:e,pass:p,pais:pa,plan:'gratis'};
  usuarios.push(u);localStorage.setItem('aion_u',JSON.stringify(usuarios));
  cerrar('Reg');activarUser(u);
  toast('✅ ¡Bienvenido a AI-ON Mentoría, '+n.split(' ')[0]+'!','ok');
}
function login(){
  const e=document.getElementById('lEmail').value.trim();
  const p=document.getElementById('lPass').value;
  const err=document.getElementById('errLogin');
  if(!e||!p)return mostrarErr(err,'Completá todos los campos.');
  const u=usuarios.find(u=>u.email===e&&u.pass===p);
  if(!u)return mostrarErr(err,'Email o contraseña incorrectos.');
  cerrar('Login');activarUser(u);
  toast('✅ ¡Bienvenido de vuelta!','ok');
}
function activarUser(u){
  userActual=u;localStorage.setItem('aion_ua',JSON.stringify(u));
  document.getElementById('userAv').textContent=u.nombre[0].toUpperCase();
  document.getElementById('userName').textContent=u.nombre.split(' ')[0];
  document.getElementById('authBtns').style.display='none';
  document.getElementById('userPill').style.display='flex';
}
function logout(){
  userActual=null;localStorage.removeItem('aion_ua');
  document.getElementById('authBtns').style.display='flex';
  document.getElementById('userPill').style.display='none';
  toast('👋 Sesión cerrada','ok');
}
function abrir(t){document.getElementById('m'+t).classList.add('open');}
function cerrar(t){document.getElementById('m'+t).classList.remove('open');}
function cambiar(c,a){cerrar(c);abrir(a);}
document.querySelectorAll('.modal-bg').forEach(b=>b.addEventListener('click',e=>{if(e.target===b)b.classList.remove('open');}));
function mostrarErr(div,msg){div.textContent=msg;div.style.display='block';setTimeout(()=>div.style.display='none',4000);}
function toast(msg,tipo){const t=document.getElementById('toast');t.textContent=msg;t.className='toast '+tipo+' show';setTimeout(()=>t.classList.remove('show'),3500);}
const obs=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting){e.target.style.opacity='1';e.target.style.transform='translateY(0)';}});},{threshold:0.08});
document.querySelectorAll('.mod-card,.plan-card,.cv-card,.hs').forEach(el=>{el.style.opacity='0';el.style.transform='translateY(14px)';el.style.transition='opacity .5s ease,transform .5s ease';obs.observe(el);});
if(userActual)activarUser(userActual);
</script>
</body>
</html> 
